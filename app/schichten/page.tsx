// Phase 2 — wird nach Abnahme von Tab 2 (Kalender) implementiert.
export default function SchichtenPage() {
  return (
    <main className="min-h-screen sf-page flex items-center justify-center px-4">
      <p className="text-sm sf-text-3">Schichten — kommt nach Tab 2.</p>
    </main>
  )
}
